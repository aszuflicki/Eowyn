const serverSettings = {
    port: 8001
}

const dbSettings = {
    user: "priceservice",
    password: "priceservice123",
    url: "localhost",
    port: 5432,
    dbname: "prices"
}

module.exports = { serverSettings, dbSettings }

